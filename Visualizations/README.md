# Visualizations

This folder contains figure outputs of the SBF model results saved in `.html` and `.pdf` format.  

Included:
- **component_weights** — figure showing component spatial loadings across modalities.  
- **prediction_weights** — figure showing prediction weights for CDR-SOB.  

These HTML files are static figures that can be opened in any web browser for viewing.
